# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
  - master

pool:
  vmImage: ubuntu-latest

steps:
  - script: echo Hello, world!
    displayName: 'Run a one-line script'

 - checkout: self             #Set to self in order for the parent repo to clone
      submodules: true           #This must be set to true
      persistCredentials: true   #I'm not sure if this line is needed 

  - task: AzureStaticWebApp@0
    inputs:
      app_location: '/'
      output_location: 'build'
      azure_static_web_apps_api_token: '88869188c7c4aaf4f7fbaea54d09bdb17113adaba993e88c2b40cbb50e15e1642-770158a9-e108-44f0-b69e-326347cd71f8010140695'
